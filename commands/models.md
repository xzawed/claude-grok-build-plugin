---
description: List available Grok models
---

Call `grok_cli` with `args: ["models"]`. Present the model list from `stdoutTail`; on error
show `stderrTail`. If `status` is `blocked`, relay the `message`.
