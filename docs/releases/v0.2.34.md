# v0.2.34 — 상대 경로 `GROK_HOME`을 grok처럼, grok이 실행될 폴더 기준으로 푼다 (A35)

2026-09-24. `GROK_HOME`을 상대 경로로 두면 grok은 그것을 **자기 작업 폴더** 기준으로 푼다. 이 플러그인은
그동안 **묻는 프로세스**(MCP 서버·hook)의 폴더 기준으로 풀었다. 그래서 둘이 서로 다른 홈을 봤다.
세션이 `<작업 폴더>/rel-home`에 있는데도 위임은 "로그인 필요"로 거절됐다(서버와 hook 둘 다). 거꾸로
`billingCaveat`와 resume 세션 색인도 grok이 읽지 않는 파일을 읽었다. 오너가 "grok과 같게 풀기"를 골랐다.

| 잰 것 (grok 1.0.41, win32, `grok du --json`, 쿼터 0) | 답 |
|---|---|
| 상대 `GROK_HOME`은 무엇 기준인가 | **grok의 작업 폴더** — `--cwd`가 있으면 그 폴더(띄운 폴더보다 이긴다), 없으면 띄운 폴더 |
| `~`를 푸나 | **아니다** — `~/.x`는 `<폴더>\~\.x`다 |
| `GROK_HOME`이 없으면 무엇이 홈을 정하나 | win32에서 **`USERPROFILE`**. `HOME`은 아니다(둘 다 바꾸면 `USERPROFILE`이 이긴다) |
| `grok login`도 그런가 | `login --help`만으로 `<띄운 폴더>\rel-home`이 생겼다 — 실제 로그인은 재지 않았다 |

---

## 무엇이 바뀌나

grok 대신 홈을 찾는 곳은 이제 모두 **grok이 실행될 폴더**를 기준으로 묻는다(`env.ts` `grokHomeFor`).
절대 경로 `GROK_HOME`(Windows는 `C:\…`처럼 드라이브부터, 또는 UNC)과 미설정은 전과 똑같다 — 폴더와 무관하기
때문이다. Windows의 드라이브 없는 `\x`·`/x`는 Node가 절대 경로라 부르지만 grok은 작업 폴더의 드라이브에 두므로,
이제 폴더를 따라 푼다(아래 "머지 전 검토").

- **위임 3종(delegate·plan·verify)** — 인증 사전 확인과 `billingCaveat`가 작업 폴더(`cwd`) 기준이다.
  resume 세션 색인은 grok이 실제로 뜨는 폴더 기준이다.
- **`worktree: true`** — grok은 `~/.grok-build/worktrees` 아래 **새 폴더**에서 뜬다. 상대 `GROK_HOME`이 그
  안(새 checkout)을 가리키면 세션이 있을 수 없으므로, worktree를 만들기 **전에** 거절한다. 전에는 작업 폴더를
  확인해 통과시키고, worktree를 만들고, grok을 띄운 뒤 `auth_error`와 `grok login` 안내로 끝났다. 그 안내로는
  고칠 수 없었다 — 다음 worktree도 새 폴더다. 확인은 호출마다 새 이름의 가상 폴더로 한다. 아무것도 미리 있을
  수 없게 하려는 것이다(아래 "머지 전 검토"). 밖으로 나가는 경로(`../x`)는 이름과 무관한 실제 경로라 그대로 확인한다.
- **hook** — 호출의 `cwd`로 찾는다. grok의 폴더를 알 수 없는 호출은 **추측하지 않고 통과시킨다**. `cwd` 없는
  `grok_cli`, 인자에 `--cwd`가 있는 `grok_cli`, `worktree: true` 위임이 그렇다. 이 hook의 원칙은 "정상 호출을
  절대 막지 않는다"이다. 위임은 서버의 사전 확인이 다시 본다. **`grok_cli`에는 서버 확인이 없으므로** 그 두 경우에
  남는 것은 grok 자신의 확인뿐이다 — 상대 경로에서 프롬프트를 실은 `grok_cli`는 `cwd`를 넘기고 인자에 `--cwd`를
  넣지 않아야 hook이 확인한다.
- **`grok_build_status`·`grok_auth_check`** — `cwd`를 받으면 그 폴더 기준으로 답한다(`grok_auth_check`는 새
  입력). 상대 경로면 **`grokHomeNote`** 가 붙어 어느 폴더의 홈 기준인지 말한다. 준비 상태를 묻는 명령 템플릿
  8개·에이전트·스킬 2개가 작업할 절대 `cwd`를 넘기도록 바뀌었다(테스트로 고정). `/grok:status`만은 넘기지
  않는다 — 그 `cwd`는 사용량 필터이기도 해서 대시보드가 좁아진다. 대신 메모를 보여준다.
- **거절 메시지** — 상대 경로에서 로그인 확인이 실패하면 메시지 뒤에 같은 메모가 붙는다(서버·hook 둘 다).
  `grok login`만 안내하면 로그인은 사용자 터미널의 폴더에 떨어진다.

## 전후 실측 (배포 번들, 합성 세션·가짜 키, 쿼터 0)

세션은 작업 폴더 Y의 `rel-home`에만 있고, MCP 서버와 hook은 다른 폴더 X에서 뜬다. `GROK_HOME=rel-home`.

| 경우 | v0.2.33 | v0.2.34 |
|---|---|---|
| hook — 위임 `cwd: Y` | **거부** "구독 로그인이 필요합니다" | 통과 |
| status `cwd: Y` | `ready: false`, caveat 없음 | `ready: true`, `billingCaveat`(Y 아래 설정), `grokHomeNote` |
| 위임 `cwd: Y` | 110 ms에 거절, grok 안 뜸 | grok이 Y에서 뜸(합성 세션이라 401 → `auth_error`) |
| 위임 `cwd: Y`, `worktree: true` | 거절(우연히 맞음 — X를 봤다) | 225 ms에 거절, worktree 0개, 메시지가 새 worktree 안의 홈을 말함 |
| hook — `grok_cli --cwd Y -p x`, tool `cwd: Z` | 거부 | 통과 |
| hook — `grok_cli -p x`, tool `cwd: Z`(세션 없음) | 거부(어느 홈인지 말 없음) | 거부 + `Z\rel-home`을 말함 |

작업 중간 판(worktree를 몰랐던 첫 수정)은 worktree 위임을 통과시켰다. 그러자 worktree 1개가 남고 grok이 세션 없이
떠 `auth_error`로 끝났다. 그래서 worktree를 따로 다뤘다.

## 하지 않는 것

- **`~`를 풀지 않는다.** grok이 풀지 않기 때문이다. 풀면 사용자의 의도와는 맞고 grok과는 어긋난다 — 그게 이
  결함이었다.
- **상대 경로를 막지 않는다.** 폴더마다 다른 홈을 일부러 쓰는 사용자는 그대로 동작한다. 그 사용자는 작업 폴더에서
  `grok login`을 하면 된다.
- **POSIX는 재지 않았다.** 위 표는 win32 실측이다. `GROK_HOME`이 없을 때 POSIX에서 무엇이 홈을 정하는지는
  미측정이다.
- **상대 경로에서 `cwd` 없이, 또는 인자에 `--cwd`를 넣고 프롬프트를 실은 `grok_cli`는 hook이 확인하지 않는다.**
  grok이 어느 폴더에서 뜰지 hook이 알 수 없기 때문이다. `grok_cli`에는 서버 확인이 없으니 그때는 grok 자신의 확인만
  남는다. `/grok:cli` 템플릿은 프롬프트 실행에 `cwd`를 넘긴다.
- **닿지 않는 네트워크 경로를 `cwd`로 주면 확인이 네트워크 시간초과를 기다린다**(반례 검토 실측 약 21초). 홈이
  폴더에 따라 달라질 때만이다 — 드라이브까지 있는 절대 경로나 미설정이면 작업 폴더를 건드리지 않는다.
- **없는 `cwd`로 위임하면 "cwd가 없다"보다 먼저 "로그인 필요"가 나온다**(홈이 폴더에 따라 달라질 때만). 인증 사전
  확인이 cwd 검증보다 먼저 돌고, 없는 폴더 아래에는 세션이 없기 때문이다. 메시지 뒤의 `grokHomeNote`가 그 없는
  폴더를 가리킨다(재검토가 배포 번들로 확인).
- **끝 공백·점의 Windows 정규화는 다루지 않는다** — `docs/10` A36(열림). `GROK_HOME` 끝의 공백은 A35 전부터 있던
  결함이다. 작업 폴더 이름 끝의 공백·점은 홈이 폴더에 따라 달라질 때 이 릴리스부터 같은 모양으로 걸린다.

## 머지 전 검토

- **Grok 반증 (hook).** "정상 호출을 hook이 막는 호출 모양이 있다"는 주장을 호출 모양 5개 × `GROK_HOME` 종류로
  **분류**시켰다. 결과는 `CLAIM_NOT_SHOWN`, 5개 모두 "잘못 막지 않음"이다.
- **Grok 반증 (서버) — 진짜 결함 하나.** 같은 방식으로 조회 4개를 분류시켰더니 worktree 행이 `CAN_DIFFER`였다.
  첫 판의 가상 폴더는 고정 이름 `(새 worktree)`였다. 그 폴더가 세션과 함께 존재하면 확인은 통과한다. 그런데 grok은
  다른 새 폴더에서 뜬다. 게다가 **거절 메시지가 바로 그 경로를 출력했다** — 사용자가 거기서 로그인하라고 초대하는
  셈이었다. 실측으로 확인했다: 그 폴더에 합성 세션을 심자 worktree 1개가 생기고 grok이 떠 `auth_error`로 끝났다.
  가상 폴더를 호출마다 새 이름으로 바꾸자 같은 상태에서 225 ms에 거절되고 아무것도 생기지 않았다.

- **독립 검토자 셋(코드·테스트 / 문서 / 반례) — 두 번째 커밋.** 지적은 전부 다른 방법으로 다시 확인한 뒤 고쳤다.
  - **hook이 읽을 수 없는 페이로드를 통과시켰다**(상대 경로일 때 — main은 거부했다). 폴더를 모르면 추측하지 않는다는
    규칙이 "읽을 수 없으면 닫힌 쪽으로"라는 약속까지 삼켰다. 이제 그런 페이로드는 A35 이전처럼 확인한다.
  - **Windows의 드라이브 없는 루트 경로**(`\x\gh`)를 절대 경로로 다뤘다. grok은 그것을 작업 폴더의 드라이브에 둔다
    (`grok du`로 재현: C:에서 `C:\…`, D:에서 `D:\…`). 이제 상대 경로처럼 폴더를 따라 푼다.
  - **실제 배선을 지키는 테스트가 없었다** — 주입을 빼도 유닛 테스트가 전부 녹색이었다. 배포 번들을 띄우는 e2e
    테스트를 넣었고, 배선을 빼면 그 테스트가 실패하는 것을 확인했다.
  - **거절 메모가 엉뚱한 거절에도 붙었다**("grok 미설치", api 모드의 "키 없음"). 이제 세션이 없다는 거절에만 붙는다.
  - **문서:** `grok_cli`에도 서버 확인이 있는 것처럼 쓴 곳 4곳, `cwd`를 넘기지 않던 템플릿 3곳(setup·tour·
    first-mile — 이제 테스트로 고정), A7 이후 낡은 "미설정이면 서버가 막는다" 3곳 등을 고쳤다.
  - 새로 찾았지만 이 릴리스 밖에 둔 것: 끝 공백 정규화(A36으로 열었다), 위 "하지 않는 것"의 항목들.
- **그 수정의 재검토 — 수정이 결함을 하나 들여왔다.** 드라이브 없는 경로를 가리는 첫 판정식이 끝 구분자 없는 공유
  루트(`\\srv\share`)까지 "폴더에 따라 달라짐"으로 봐서, main이 거부하던 `grok_cli` 프롬프트 실행이 통과했다. 판정을
  "절대 경로가 아니거나 구분자 하나로 시작"으로 바꿨고, 새 번들에서 같은 입력이 다시 거부된다.

## 새로 연 것 — B7

모델 자체 키(`config.toml`)가 거부될 때, 래퍼는 `grok login`을 안내하는데 grok 자신은 *"no need to run /login"*
이라고 말했다. 합성 세션으로는 거부된 것이 키인지 세션인지 가를 수 없다. 실세션(컨테이너 전용 로그인)이 필요하다
— `docs/10` B7.

---

## 재현과 수락

- grok 쪽 실측 전문: `docs/specs/grok-cli-contract.md` §8.
- 수락: `accept-release.mjs`에 `A35` 칸이 생겼다. 상대 `GROK_HOME` 아래 빈 세션 파일을 둔 임시 작업 폴더를 status에
  넘기고, `ready`가 그 폴더 기준이고 서버 폴더 기준이 아닌지, `grokHomeNote`가 붙는지 본다(spawn 없음).
  수정이 없는 0.2.33 설치본은 이 칸만 실패한다(12/13).
