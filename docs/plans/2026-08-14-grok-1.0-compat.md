# Plan: v0.2.7 Grok 1.0.3 호환

> **SHIPPED — 이력 문서. 실행하지 말 것.** 이 플랜은 `v0.2.7`로 출하됐다. 아래 항목은 당시
> 계획의 기록이며 남은 일이 아니다. 현재 동작의 원천은 `docs/specs/grok-cli-contract.md`(실측)와
> `mcp-server/src/`다. *(주석 2026-09-05.)*

설계: [`docs/specs/2026-08-14-grok-1.0-compat-design.md`](../specs/2026-08-14-grok-1.0-compat-design.md)

## Task 1 — 성공 판정

- `isSuccessfulStopReason` + 유닛 (`end_turn`→completed, `EndTurn` 유지, `cancelled` 실패)
- `classifySpawnResult`가 이 함수만 사용

## Task 2 — verify / best_of_n / model alias

- `--check` 제거, `VERIFY_PROMPT_SUFFIX` 부착, 테스트가 argv에 `--check` 없음을 단언
- 모든 `bestOfN` spawn 거절
- `grok-build` → `--model` 생략

## Task 3 — grok_cli import + HOME

- `import` blocked + 메시지 테스트
- `buildGrokEnv` HOME 폴백 테스트

## Task 4 — 문서 / 버전

- `docs/specs/grok-cli-contract.md` 1.0.3 재실측으로 교체 (포인터는 이 파일)
- `docs/04` verify/plan/stopReason, 커맨드·스킬·README 한 줄
- `0.2.7`: package.json, plugin.json, version.ts fallback, `docs/releases/v0.2.7.md`,
  `CLAUDE.md`, `docs/09`, CHANGELOG

## Task 5 — 게이트 + 라이브

- `npm test` / `typecheck` / `build`
- 격리 저장소에서 delegate / plan / verify / worktree / resume / cli / auth 재실측
